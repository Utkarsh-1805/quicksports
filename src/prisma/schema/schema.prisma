// Main Prisma schema file - contains datasource and generator configurations

generator client {
  provider   = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "postgresql"
}